'use client';import {useEffect,useState} from 'react';import {Shell,Top} from '../../components/Shell';import {api} from '../../lib/api';export default function Candidates(){const [x,setX]=useState<any[]>([]);useEffect(()=>{api('/candidates').then(setX)},[]);return <Shell><Top title="Candidates"/><div className="p-9"><div className="card overflow-hidden"><table className="w-full text-sm"><thead><tr className="bg-gray-50 text-left"><th className="p-4">Candidate</th><th>Phone</th><th>Interviews</th></tr></thead><tbody>{x.map(c=><tr className="border-t" key={c.id}><td className="p-4 font-semibold">{c.name}</td><td>{c.phone_number}</td><td>{c.interviews}</td></tr>)}</tbody></table></div></div></Shell>}
